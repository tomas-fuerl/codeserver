# MIG-002: Sauberen eigenständigen Arbeitsbaum erzeugen

- **Status:** COMPLETED
- **Exportdatum:** 2026-07-20 (UTC)

## Ziel

Aus dem privaten Homelab-Repository wird ein eigenständiger, sanitiserter,
whitelist-basierter und history-freier Codeserver-Arbeitsbaum erzeugt. Dieser
Task erzeugt ausschließlich den lokalen Arbeitsbaum und beginnt MIG-003 nicht.

## Quelle und Ziel

| Merkmal | Wert |
| --- | --- |
| Quelle | `/config/workspace/Homelab` |
| Ziel | `/config/workspace/codeserver` |
| Quellbranch | `chore/codeserver-consolidation-audit` |
| Ausgangs-HEAD | `dbff9a73b193268fd1ad69d5e657fdbb0b6b1c79` |
| Ausgangs-Commitanzahl | 44 |
| Exportskript | `scripts/security/create-public-snapshot.sh` |
| Git-Initialisierung | keine |
| Exportierte Dateianzahl | 32 |

## Ausgeschlossene Kategorien

- `.git`, Git-Objekte und Git-Refs;
- Environment-Dateien außer `.env.example`, insbesondere `.env`, `stack.env`,
  `*.env` und `.env.*`;
- Secrets und Passwort-Hashes;
- Backups und Restore-Daten;
- Logs und Archive;
- Datenbanken;
- Schlüssel und Zertifikate;
- produktive Portainer-Werte;
- DSM-, Firewall- und Reverse-Proxy-Konfiguration;
- nicht freigegebene Homelab-Bereiche und benutzerspezifische Konfiguration.

## Ausgeführte Prüfungen

- Quellpfad, Zielpfad und deren Trennung geprüft;
- Branch, HEAD, Commitanzahl und Quellstatus erfasst;
- Zielpfad war vor Beginn nicht vorhanden;
- MIG-001 war `READY_FOR_REVIEW` und wurde nach genehmigtem menschlichem
  Review auf `COMPLETED` gesetzt;
- Snapshot-Skript mit `bash -n` und ShellCheck erfolgreich geprüft;
- Snapshot ohne `--init-git` erfolgreich erzeugt;
- Artefaktprüfung: 0 Git-Artefakte, 0 unzulässige Environment-Dateien, 0
  sensible Dateitypen, 0 Symlinks und 0 fremde Homelab-Bereiche;
- pfadbasierte Privatwertsuche mit ausschließlich relativen Dateipfaden als
  Ausgabe durchgeführt;
- keine privaten IPv4-Werte aus den geprüften Bereichen `192.168.0.0/16`,
  `10.0.0.0/8` oder `172.16.0.0/12` gefunden.

Die Privatwertprüfung des Ausgangslaufs fand jedoch zwei verpflichtende Blocker
und wurde daher vorschriftsgemäß beendet. Publication-Audit, Syntax- und
ShellCheck-Prüfung aller exportierten Shellskripte, formales Dateiinventar und
finaler Auditlauf wurden im Ausgangslauf nach dem vorgeschriebenen sicheren
Stopp nicht ausgeführt.

## Befunde

- Der Snapshot umfasste vor Übernahme der Migrationssteuerung 25 Dateien.
- Fünf ausdrücklich freigegebene Migrationsdokumente wurden identisch aus der
  Quelle übernommen.
- Das Herkunftsmanifest und dieses Taskdokument ergänzen den final erwarteten
  Zielbestand auf 32 Dateien.
- Der Zielbaum enthält kein initialisiertes Git-Repository.
- Eine reale Domainkennung wurde in `extensions.lock.txt` gefunden; der Wert
  wird als `[REDACTED]` dokumentiert.
- Ein produktiver absoluter Synology-Pfad wurde in
  `docs/migration/MIGRATION-PLAN.md` gefunden; der Wert wird als `[REDACTED]`
  dokumentiert.
- Referenzen auf `stack.env` und `hashed_password` wurden ausschließlich als
  betroffene Dateipfade erfasst; keine Werte oder Trefferinhalte wurden
  ausgegeben.
- MIG-002 war vor Remediation 1 deshalb `BLOCKED` und nicht für Review bereit.

## Externe Aktionen

Status: `NOT_REQUIRED`. Es wurde keine externe Aktion ausgeführt. Insbesondere
erfolgten kein Netzwerkzugriff, kein Remotezugriff und keine Änderung an
GitHub, Docker, Portainer, DSM, Reverse Proxy, Firewall oder Produktion.

## Abschlusskriterien

- MIG-001 ist `COMPLETED`;
- Zielbaum existiert am exakt vorgegebenen Pfad;
- keine alte Git-Historie und keine verbotenen Artefakte;
- keine realen privaten Werte oder produktiven absoluten Pfade;
- Publication-Audit mit 0 Blocker, 0 High, 0 Incomplete und Exit-Code 0;
- `bash -n` und ShellCheck für alle exportierten Shellskripte erfolgreich;
- deterministisches Inventar und identische Migrationsdokumentkopien;
- MIG-002 steht nach erfolgreicher Prüfung auf `READY_FOR_REVIEW`.

Nach Remediation 1 sind Privatwertfreiheit und Publication-Audit erfüllt. Die
Prüfungen aller exportierten Shellskripte wurden vollständig ausgeführt, sind
jedoch fehlgeschlagen; das Kriterium `READY_FOR_REVIEW` ist daher nicht erfüllt.

## Offene Risiken nach Remediation 1

- `scripts/verify-installation.sh` enthält einen bereits exportierten
  Syntaxfehler und besteht `bash -n` nicht.
- ShellCheck meldet Diagnosen in `scripts/backup-config.sh`,
  `scripts/restore-config.sh` und `scripts/verify-installation.sh`.
- Fachliche Skriptänderungen sind für Remediation 1 nicht freigegeben; der
  Zielbaum bleibt deshalb blockiert und darf nicht als Basis für MIG-003
  verwendet werden.

## Remediation 1

### Ausgangsblocker und Sanitisierung

- Die einmalige private Domainkennung stand ausschließlich in einer optionalen
  Metadaten-Kopfzeile von `extensions.lock.txt`. Nur der Hostname wurde durch
  `extensions.example.com` ersetzt.
- Fünf produktive absolute Synology-Pfadreferenzen in
  `docs/migration/MIGRATION-PLAN.md` wurden durch Pfade unter
  `<SYNOLOGY_DOCKER_ROOT>` ersetzt. Der symbolische Root ist einmal eindeutig
  definiert; Container- und Secret-Mountziele blieben unverändert.
- Alle neun Extension-IDs, Publisher-IDs, Extension-Namen und Versionen blieben
  unverändert. Die Datei enthält keine Prüfsummenfelder; es wurde keine
  Prüfsumme ergänzt oder verändert.
- Keine Secret- oder Environment-Datei wurde gelesen und keine Git-Historie
  übernommen.

### Wiederholte Prüfungen

- Privatwertsuche: keine reale private Domainkennung, kein produktiver absoluter
  Synology-Pfad und keine lokale private IPv4-Adresse. Die drei generischen
  RFC1918-Netzreferenzen stehen ausschließlich in diesem Prüfnachweis.
  Environment-Begriffe erscheinen nur als ausgeschlossene Dateinamen oder
  Konfigurationsverträge; unzulässige Environment-Dateien: 0.
- Verbotene Artefakte: 0 Git-Artefakte, 0 sensible Dateitypen, 0 Symlinks und 0
  fremde Homelab-Bereiche.
- Publication-Audit `--current-tree`: Blocker 0, High 0, Incomplete 0, Exit-Code 0. Das Audit-Skript blieb
  unverändert.
- `bash -n`: 10 Skripte geprüft, 1 Fehler in
  `scripts/verify-installation.sh`.
- ShellCheck: 10 Skripte geprüft, Exit-Code 123 über `xargs`, 84 Diagnosen in
  `scripts/backup-config.sh`, `scripts/restore-config.sh` und
  `scripts/verify-installation.sh`.

### Finales Inventar

- Gesamt: 32 reguläre Dateien.
- Root: 10 Dateien (`.env.example`, `.gitignore`, `Dockerfile`,
  `GHCR-RELEASE.md`, `LICENSE`, `README.md`, `docker-compose.ghcr.yaml`,
  `docker-compose.yaml`, `extensions.lock.txt`, `extensions.txt`).
- `.github/`: 1 Datei; `docs/`: 11 Dateien; `scripts/`: 10 Dateien.
- Shellskripte: 10; Markdown-Dokumente: 13; Workflowdateien: 1.

### Ergebnis

- Externe Aktionen: `NOT_REQUIRED`.
- MIG-001 bleibt `COMPLETED`; MIG-002 bleibt wegen der fehlgeschlagenen
  Shellprüfungen `BLOCKED`; MIG-003 bleibt `NOT_STARTED`.
- MIG-002 ist nicht für Review bereit.
- Es erfolgten kein Commit, keine Remoteänderung, kein Netzwerkzugriff und
  keine externe oder produktive Änderung. MIG-003 wurde nicht begonnen.

## Remediation 2

### Referenzprüfung und Differenzklassifikation

- Die drei Quellreferenzen unter `codeserver/scripts/` bestanden jeweils
  `bash -n` und ShellCheck. Ihre Hashes blieben während der Remediation
  unverändert.
- Vor der Reparatur unterschieden sich 14 Zeilen in `backup-config.sh`
  (3 erwartete Sanitisierungen, 11 versehentliche Shellschäden), 7 Zeilen in
  `restore-config.sh` (3 erwartete Sanitisierungen, 4 versehentliche
  Shellschäden) und 21 Zeilen in `verify-installation.sh` (8 erwartete
  Sanitisierungen beziehungsweise öffentliche Platzhalter, 13 versehentliche
  Shellschäden).
- Die Quellskripte wurden nicht vollständig übernommen. Private Pfade und die
  private Repositoryreferenz wurden nicht in den Zielbaum zurückkopiert.

### Syntaxursache und maßgebliche ShellCheck-Diagnosen

- Zwei Anfänge mehrzeiliger Command-Substitutionen in
  `scripts/verify-installation.sh` waren durch `CHANGE_ME` ersetzt worden,
  während ihre Fortsetzungen und schließenden Klammern erhalten blieben.
- Die ursprüngliche menschenlesbare ShellCheck-Ausgabe enthielt 84
  SC-Code-Nennungen. Im strukturierten Format waren dies 81 tatsächliche
  Diagnosen mit `SC1046`, `SC1047`, `SC1072`, `SC1073`, `SC2221`, `SC2222`
  und `SC2317`.
- Nach der Syntaxkorrektur blieben 77 tatsächliche Diagnosen:
  75-mal `SC2317` in `scripts/backup-config.sh`, je einmal `SC2221` und
  `SC2222` in `scripts/restore-config.sh` sowie 0 Diagnosen in
  `scripts/verify-installation.sh`.
- `SC1046`, `SC1047`, `SC1072` und `SC1073` waren Folgefehler des
  Syntaxschadens.
- Nach der vollständigen minimalen Reparatur verbleiben 0
  ShellCheck-Diagnosen.

### Technische Reparaturen

- `scripts/backup-config.sh`: beschädigte Modus-, Eigentümer-, Image-,
  Status-, Staging- und Manifestzuweisungen wiederhergestellt; private
  Installationspfade durch gequotete `CODESERVER_*`-Variablen mit
  `CHANGE_ME`-Sentinel ersetzt; interne Prüfvariablen auditneutral benannt.
  Gegenüber dem ursprünglichen Zielstand: 35 hinzugefügte und 27 entfernte
  Zeilen.
- `scripts/restore-config.sh`: `safe.directory`, Imageabfrage,
  temporäre Dateivorlage und Zähler wiederhergestellt; produktive
  Schutzpfade über `SYNOLOGY_DOCKER_ROOT`, `CODESERVER_CONFIG_ROOT` und
  `CODESERVER_SECRET_ROOT` konfigurierbar gemacht und vor Docker- oder
  Dateisystemoperationen validiert. Gegenüber dem ursprünglichen Zielstand:
  19 hinzugefügte und 7 entfernte Zeilen.
- `scripts/verify-installation.sh`: Docker-Go-Templates, Git-Sicherheitsoption,
  Secret-Mount- und Environment-Prüfungen sowie Synology-Marker
  wiederhergestellt; lokale Pfade und Referenz-URL durch gequotete
  `CODESERVER_*`-Variablen mit Sentinel-Abbruch ersetzt; auditneutrale interne
  Variablennamen verwendet. Gegenüber dem ursprünglichen Zielstand:
  35 hinzugefügte und 24 entfernte Zeilen.
- Backupformat, Secret-Archivpfad, Restore-Schutzregeln, No-op- und
  Runtime-Prüfsemantik sowie PASS-, WARN-, FAIL- und Exit-Code-Verhalten
  blieben erhalten.

### Abschlussprüfungen

- `bash -n`: 10 von 10 Zielskripten erfolgreich.
- ShellCheck: 10 von 10 Zielskripten erfolgreich, 0 Diagnosen.
- Publication-Audit `--current-tree`: Blocker 0, High 0, Incomplete 0,
  Exit-Code 0; Audit-Skript unverändert.
- Privatwert- und Artefaktprüfung: keine reale private Domain, kein
  produktiver absoluter Synology-Pfad, keine lokale private IPv4-Adresse,
  keine verbotenen Environment-, Secret-, Schlüssel-, Backup-, Archiv-, Log-
  oder Datenbankartefakte, keine Symlinks und keine fremden Homelab-Bereiche.
- Nicht-Scope-Hashvergleich: identisch.
- Externe Aktionen: `NOT_REQUIRED`; keine produktiven Kommandos ausgeführt.

### Ergebnis und Restrisiken

- MIG-001 bleibt `COMPLETED`; MIG-002 ist `READY_FOR_REVIEW`; MIG-003 bleibt
  `NOT_STARTED` und wurde nicht begonnen.
- Die interimistischen Environment-Variablennamen benötigen in MIG-004 noch
  den vorgesehenen finalen Betriebsvertrag.
- Backup, Restore, Docker, Deployment und produktive Laufzeit wurden in dieser
  Remediation bewusst nicht ausgeführt. Ihr Erfolg wird nicht vorausgesetzt.
- Es erfolgten kein Commit, keine Remoteänderung, kein Netzwerkzugriff und
  keine externe oder produktive Änderung.

## Rollback- und Abbruchstrategie

Der unveröffentlichte Zielbaum bleibt bei einem Abbruch zur redigierten
Diagnose erhalten und wird nicht automatisch gelöscht oder überschrieben.
Der Homelab-Quellbestand wird nicht zurückgesetzt. Bei einem vorgeschriebenen
Blocker wird MIG-002 auf `BLOCKED` gesetzt und sicher gestoppt.

## Menschliche Reviewentscheidung

- Das Publication-Audit war mit 0 Blockern, 0 High-Befunden und 0
  unvollständigen Prüfungen erfolgreich.
- Shellsyntax und ShellCheck waren für alle zehn exportierten Shellskripte
  vollständig erfolgreich.
- Der Zielbaum enthielt keine privaten Werte und keine ausgeschlossenen
  Artefakte.
- MIG-002 wurde durch menschliches Review genehmigt und auf `COMPLETED`
  gesetzt.
- MIG-003 wurde zur Bearbeitung freigegeben.
